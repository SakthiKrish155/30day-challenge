Find if the given string is palindrome or not;

CODE:
#include <bits/stdc++.h>
using namespace std;
int main() {
	string str;
	cin >> str;
	string s1;
	s1 = str;
	reverse(s1.begin(),s1.end());
	if(s1 == str)
		cout << "YES";
	else
		cout << "NO";	
}
