Given an array of elements , return the index of the target element. If element not found return -1

CODE:
int search(vector<int> &nums, int target)
{
   int index = 0;
   for(int i = 0 ; i < nums.size();i++)
   {
       if(nums[i] == target)
       {
           index = i;
           return index; 
       }
   }
   return -1;
}
